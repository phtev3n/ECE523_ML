batch_size: 1
device: cuda
epochs: 1
image_size:
- 256
- 256
learning_rate: 0.0003
loss:
  heatmap_weight: 1.0
  offset_weight: 1.0
  uncertainty_weight: 0.05
  visible_weight: 0.5
model:
  backbone: resnet50
  heatmap_sigma: 2.0
num_workers: 0
save_path: outputs/detector_smoke.pt
seed: 42
sequence_length: 12
synthetic:
  fps: 60.0
  num_sequences: 8
train_split: 0.8
